spring:
  datasource:
    url: jdbc:mysql://localhost:3306/documentdb?useSSL=false&serverTimezone=UTC
    username: collab
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8082

#springdoc:
 # api-docs:
  #  path: /api-docs
  #swagger-ui:
    #path: /swagger-ui.html

#Swagger UI : http://localhost:8082/swagger-ui.html 