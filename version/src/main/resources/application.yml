server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/versiondb?useSSL=false&sallowPublicKeyRetrieval=true
    username: collab
    password: rootpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

    #Swagger UI: http://localhost:8083/swagger-ui.html 
